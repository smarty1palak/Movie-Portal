<h1> Shows available</h1>

<div class="col-md-8">
    <% if @movie.shows.any? %>
      <h3>Shows available (<%= @user.microposts.count %>)</h3>
      <ol class="shows">
        <%= render shows_path %>
      </ol>
    <% end %>
  </div>
